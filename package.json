{
  "name": "1xbet-login-flow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "login": "playwright test",
    "loop": "node run-loop.js",
    "excel": "node scripts/generate-excel.js",
    "test": "node --test \"tests/unit/*.test.js\""
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "dotenv": "^16.6.1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
